# Core analysis stack for final_pipeline.py.
# scikit-learn is pinned exactly: random-forest tree construction changed
# between 1.8 and 1.9, and 1.8.0 is the version that produced the numbers
# reported in the final report (all other results are version-stable).
pandas>=2.0
numpy>=1.24
matplotlib>=3.7
scikit-learn==1.8.0
scipy>=1.10
statsmodels>=0.14
# Used by the scaffold notebooks (01-05) and src/ modules only.
seaborn>=0.12
xgboost>=2.0
jupyter>=1.0
nbformat>=5.9
openpyxl>=3.1
python-docx>=1.1
